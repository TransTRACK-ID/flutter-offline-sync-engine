name: offline_sync_engine
description: >
  Full offline-first toolkit for Flutter/Dart: push queues, pull caches,
  local-first repositories, Hive/Drift storage adapters, and domain codegen.
version: 0.3.0
homepage: https://github.com/TODO-your-org/offline_sync_engine
repository: https://github.com/TODO-your-org/offline_sync_engine
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  hive: ^2.2.3
  args: ^2.6.0
  path: ^1.9.0
  yaml: ^3.1.3

dev_dependencies:
  test: ^1.24.0

executables:
  generate_domain: generate_domain
